<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard - Portfolio Inquiries</title>
  <!-- Google Fonts Outfit & Inter -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;600;700&family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  
  <style>
    :root {
      --bg-primary: #09090b;
      --bg-secondary: #0f0f13;
      --card-bg: rgba(24, 24, 27, 0.5);
      --accent: #6366f1;
      --accent-hover: #4f46e5;
      --text-main: #f4f4f5;
      --text-muted: #a1a1aa;
      --success: #10b981;
      --warning: #f59e0b;
      --danger: #ef4444;
      --border-color: rgba(255, 255, 255, 0.08);
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: 'Inter', sans-serif;
      background: linear-gradient(135deg, #07090e 0%, #030305 50%, #0d0a1c 100%);
      color: var(--text-main);
      min-height: 100vh;
      position: relative;
    }

    /* Ambient glows */
    body::before, body::after {
      content: '';
      position: absolute;
      width: 500px;
      height: 500px;
      border-radius: 50%;
      filter: blur(150px);
      z-index: -1;
      opacity: 0.1;
      pointer-events: none;
    }

    body::before {
      background: var(--accent);
      top: 10%;
      left: 5%;
    }

    body::after {
      background: #c084fc;
      bottom: 15%;
      right: 5%;
    }

    .navbar {
      border-bottom: 1px solid var(--border-color);
      background: rgba(9, 9, 11, 0.7);
      backdrop-filter: blur(16px);
      -webkit-backdrop-filter: blur(16px);
      position: sticky;
      top: 0;
      z-index: 100;
    }

    .nav-container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 18px 24px;
      display: flex;
      justify-content: flex-end;
      align-items: center;
      gap: 20px;
    }

    .brand {
      margin-right: auto;
      font-family: 'Outfit', sans-serif;
      font-size: 1.5rem;
      font-weight: 700;
      background: linear-gradient(to right, #fff, #a5b4fc);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      text-decoration: none;
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .brand-dot {
      width: 8px;
      height: 8px;
      background: var(--accent);
      border-radius: 50%;
      box-shadow: 0 0 10px var(--accent);
    }

    .nav-link {
      color: var(--text-muted);
      text-decoration: none;
      font-size: 0.95rem;
      font-weight: 500;
      transition: color 0.3s;
    }

    .nav-link:hover, .nav-link.active {
      color: var(--text-main);
    }

    .btn-logout {
      padding: 10px 18px;
      border: 1px solid var(--border-color);
      background: rgba(244, 63, 94, 0.05);
      color: #f43f5e;
      border-radius: 10px;
      font-size: 0.9rem;
      font-weight: 500;
      text-decoration: none;
      transition: all 0.3s ease;
    }

    .btn-logout:hover {
      background: #f43f5e;
      color: #fff;
      box-shadow: 0 4px 15px rgba(244, 63, 94, 0.3);
      transform: translateY(-1px);
    }

    .main-content {
      max-width: 1200px;
      margin: 0 auto;
      padding: 40px 24px;
      animation: fadeIn 0.6s cubic-bezier(0.16, 1, 0.3, 1) forwards;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(10px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .welcome-header {
      margin-bottom: 36px;
    }

    .welcome-header h2 {
      font-family: 'Outfit', sans-serif;
      font-size: 2.2rem;
      font-weight: 700;
      margin-bottom: 8px;
    }

    .welcome-header p {
      color: var(--text-muted);
      font-size: 1.05rem;
    }

    /* Stats Grid */
    .stats-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 20px;
      margin-bottom: 40px;
    }

    .stats-card {
      background: var(--card-bg);
      border: 1px solid var(--border-color);
      border-radius: 16px;
      padding: 24px;
      display: flex;
      flex-direction: column;
      backdrop-filter: blur(10px);
      -webkit-backdrop-filter: blur(10px);
    }

    .stats-title {
      font-size: 0.85rem;
      font-weight: 600;
      color: var(--text-muted);
      text-transform: uppercase;
      letter-spacing: 0.5px;
      margin-bottom: 8px;
    }

    .stats-value {
      font-family: 'Outfit', sans-serif;
      font-size: 2.5rem;
      font-weight: 700;
      color: #fff;
    }

    /* Search Bar */
    .controls-panel {
      margin-bottom: 30px;
      background: rgba(24, 24, 27, 0.3);
      border: 1px solid var(--border-color);
      border-radius: 16px;
      padding: 20px;
      backdrop-filter: blur(10px);
      -webkit-backdrop-filter: blur(10px);
    }

    .search-form {
      display: flex;
      gap: 12px;
    }

    .search-input {
      flex: 1;
      padding: 12px 16px;
      background: rgba(9, 9, 11, 0.6);
      border: 1px solid var(--border-color);
      border-radius: 10px;
      color: #fff;
      font-family: inherit;
      font-size: 0.95rem;
      transition: all 0.3s;
    }

    .search-input:focus {
      outline: none;
      border-color: var(--accent);
      box-shadow: 0 0 0 3px rgba(99, 102, 241, 0.15);
    }

    .btn-search {
      padding: 12px 24px;
      background: var(--accent);
      color: #fff;
      border: none;
      border-radius: 10px;
      font-family: inherit;
      font-weight: 600;
      font-size: 0.95rem;
      cursor: pointer;
      transition: all 0.3s;
    }

    .btn-search:hover {
      background: var(--accent-hover);
    }

    .btn-clear-search {
      padding: 12px 20px;
      background: transparent;
      color: var(--text-muted);
      border: 1px solid var(--border-color);
      border-radius: 10px;
      font-family: inherit;
      font-weight: 500;
      font-size: 0.95rem;
      text-decoration: none;
      text-align: center;
      transition: all 0.3s;
    }

    .btn-clear-search:hover {
      border-color: var(--text-muted);
      color: #fff;
    }

    /* Message Cards */
    .section-title {
      font-family: 'Outfit', sans-serif;
      font-size: 1.4rem;
      font-weight: 600;
      margin-bottom: 20px;
      display: flex;
      align-items: center;
      gap: 12px;
    }

    .badge-count {
      background: rgba(99, 102, 241, 0.15);
      color: var(--accent);
      padding: 2px 10px;
      border-radius: 20px;
      font-size: 0.85rem;
      font-weight: 600;
    }

    .requests-list {
      display: flex;
      flex-direction: column;
      gap: 20px;
      margin-bottom: 30px;
    }

    .request-card {
      background: var(--card-bg);
      border: 1px solid var(--border-color);
      border-radius: 18px;
      padding: 28px;
      position: relative;
      overflow: hidden;
      backdrop-filter: blur(10px);
      -webkit-backdrop-filter: blur(10px);
      transition: all 0.3s ease;
    }

    .request-card::before {
      content: '';
      position: absolute;
      left: 0;
      top: 0;
      bottom: 0;
      width: 4px;
      background: var(--accent);
      border-radius: 4px 0 0 4px;
      transition: width 0.2s ease;
    }

    .request-card:hover {
      border-color: rgba(99, 102, 241, 0.3);
      transform: translateY(-2px);
      box-shadow: 0 10px 20px rgba(0, 0, 0, 0.3);
    }

    .request-card:hover::before {
      width: 6px;
    }

    .request-header {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      flex-wrap: wrap;
      gap: 12px;
      margin-bottom: 20px;
    }

    .sender-info h3 {
      font-family: 'Outfit', sans-serif;
      font-size: 1.25rem;
      font-weight: 600;
      color: #fff;
      margin-bottom: 4px;
    }

    .sender-email {
      color: var(--text-muted);
      font-size: 0.9rem;
      text-decoration: none;
      transition: color 0.2s;
    }

    .sender-email:hover {
      color: var(--accent);
      text-decoration: underline;
    }

    .request-date {
      font-size: 0.85rem;
      color: var(--text-muted);
      background: rgba(255, 255, 255, 0.04);
      padding: 6px 12px;
      border-radius: 8px;
      border: 1px solid var(--border-color);
    }

    .request-subject {
      font-size: 1rem;
      font-weight: 600;
      color: #fff;
      margin-bottom: 12px;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .subject-prefix {
      color: var(--accent);
      font-size: 0.85rem;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      font-weight: 700;
    }

    .request-message {
      background: rgba(0, 0, 0, 0.2);
      border-radius: 12px;
      padding: 18px;
      color: #e4e4e7;
      font-size: 0.95rem;
      line-height: 1.6;
      border: 1px solid rgba(255, 255, 255, 0.02);
      margin-bottom: 20px;
      white-space: pre-wrap;
    }

    .actions {
      display: flex;
      justify-content: space-between;
      align-items: center;
      flex-wrap: wrap;
      gap: 12px;
    }

    .primary-actions {
      display: flex;
      gap: 12px;
    }

    .secondary-actions {
      display: flex;
      gap: 12px;
    }

    .btn-reply {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 10px 20px;
      background: linear-gradient(135deg, var(--accent) 0%, #4f46e5 100%);
      color: #fff;
      font-size: 0.9rem;
      font-weight: 600;
      border-radius: 10px;
      text-decoration: none;
      transition: all 0.3s ease;
      box-shadow: 0 4px 12px rgba(99, 102, 241, 0.2);
    }

    .btn-reply:hover {
      background: linear-gradient(135deg, #7c3aed 0%, #6366f1 100%);
      box-shadow: 0 6px 15px rgba(124, 58, 237, 0.3);
      transform: translateY(-1px);
    }

    .btn-action-form {
      display: inline;
    }

    .btn-archive, .btn-delete {
      padding: 10px 18px;
      border-radius: 10px;
      font-family: inherit;
      font-size: 0.9rem;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s ease;
      background: transparent;
      display: inline-flex;
      align-items: center;
      gap: 8px;
    }

    .btn-archive {
      border: 1px solid rgba(245, 158, 11, 0.3);
      color: var(--warning);
      background: rgba(245, 158, 11, 0.05);
    }

    .btn-archive:hover {
      background: var(--warning);
      color: #000;
      box-shadow: 0 4px 15px rgba(245, 158, 11, 0.25);
      transform: translateY(-1px);
    }

    .btn-delete {
      border: 1px solid rgba(239, 68, 68, 0.3);
      color: var(--danger);
      background: rgba(239, 68, 68, 0.05);
    }

    .btn-delete:hover {
      background: var(--danger);
      color: #fff;
      box-shadow: 0 4px 15px rgba(239, 68, 68, 0.25);
      transform: translateY(-1px);
    }

    /* Pagination */
    .pagination {
      display: flex;
      justify-content: center;
      align-items: center;
      gap: 8px;
      margin-top: 40px;
    }

    .page-link {
      padding: 8px 16px;
      border: 1px solid var(--border-color);
      border-radius: 8px;
      color: var(--text-muted);
      text-decoration: none;
      font-size: 0.9rem;
      font-weight: 500;
      transition: all 0.3s;
      background: rgba(24, 24, 27, 0.2);
    }

    .page-link:hover {
      border-color: var(--text-muted);
      color: #fff;
    }

    .page-link.active {
      background: var(--accent);
      border-color: var(--accent);
      color: #fff;
      box-shadow: 0 4px 12px rgba(99, 102, 241, 0.25);
    }

    .empty-state {
      background: var(--card-bg);
      border: 1px dashed var(--border-color);
      border-radius: 18px;
      padding: 60px 24px;
      text-align: center;
      backdrop-filter: blur(10px);
      -webkit-backdrop-filter: blur(10px);
    }

    .empty-icon {
      font-size: 3rem;
      margin-bottom: 16px;
      color: var(--text-muted);
    }

    .empty-title {
      font-family: 'Outfit', sans-serif;
      font-size: 1.4rem;
      font-weight: 600;
      margin-bottom: 8px;
    }

    .empty-desc {
      color: var(--text-muted);
      font-size: 0.95rem;
    }

    @media (max-width: 640px) {
      .navbar {
        padding: 12px 16px;
      }
      .main-content {
        padding: 24px 16px;
      }
      .request-card {
        padding: 20px;
      }
      .actions {
        flex-direction: column;
        align-items: stretch;
      }
      .primary-actions, .secondary-actions {
        justify-content: space-between;
      }
      .btn-reply, .btn-archive, .btn-delete {
        flex: 1;
        justify-content: center;
      }
    }
  </style>
</head>
<body>

  <header class="navbar">
    <div class="nav-container">
      <a href="/admin/dashboard" class="brand">
        <span class="brand-dot"></span>
        Portfolio Admin
      </a>
      <a href="/admin/dashboard" class="nav-link active">Active Inbox</a>
      <a href="/admin/archives" class="nav-link">Archives</a>
      <a href="/admin/logout" class="btn-logout">Logout</a>
    </div>
  </header>

  <main class="main-content">
    <div class="welcome-header">
      <h2>Active Inquiries</h2>
      <p>Manage pending inquiries submitted from your portfolio website</p>
    </div>

    <!-- Stats Panel -->
    <section class="stats-grid">
      <div class="stats-card">
        <span class="stats-title">Total Active Inquiries</span>
        <span class="stats-value"><%= total %></span>
      </div>
      <div class="stats-card">
        <span class="stats-title">Showing Page</span>
        <span class="stats-value" style="font-size: 1.8rem; font-family: 'Outfit', sans-serif; margin-top: auto; font-weight: 600;">
          <%= totalPages > 0 ? page + ' of ' + totalPages : '0 of 0' %>
        </span>
      </div>
    </section>

    <!-- Controls Panel -->
    <section class="controls-panel">
      <form action="/admin/dashboard" method="GET" class="search-form">
        <input 
          type="text" 
          name="search" 
          placeholder="Search inquiries by name, email, subject, or message..." 
          class="search-input"
          value="<%= search %>"
        >
        <button type="submit" class="btn-search">Search</button>
        <% if (search) { %>
          <a href="/admin/dashboard" class="btn-clear-search">Clear</a>
        <% } %>
      </form>
    </section>

    <!-- Messages List Section -->
    <section>
      <h3 class="section-title">
        Inbox List
        <span class="badge-count"><%= total %></span>
      </h3>

      <% if (requests.length === 0) { %>
        <div class="empty-state">
          <div class="empty-icon">📨</div>
          <h4 class="empty-title">
            <%= search ? 'No matching inquiries' : 'Inbox is empty' %>
          </h4>
          <p class="empty-desc">
            <%= search ? 'Try checking your search terms or refining your query.' : 'Any active contact inquiries submitted will show up here.' %>
          </p>
        </div>
      <% } else { %>
        <div class="requests-list">
          <% requests.forEach(function(req) { %>
            <div class="request-card" id="card-<%= req._id %>">
              <div class="request-header">
                <div class="sender-info">
                  <h3><%= req.name %></h3>
                  <a href="mailto:<%= req.email %>" class="sender-email"><%= req.email %></a>
                </div>
                <div class="request-date">
                  <%= new Date(req.createdAt).toLocaleString('en-US', { dateStyle: 'medium', timeStyle: 'short' }) %>
                </div>
              </div>

              <% if (req.subject) { %>
                <div class="request-subject">
                  <span class="subject-prefix">Subject:</span>
                  <span><%= req.subject %></span>
                </div>
              <% } %>

              <div class="request-message"><%= req.message %></div>

              <div class="actions">
                <div class="primary-actions">
                  <a href="mailto:<%= req.email %>?subject=Re: <%= encodeURIComponent(req.subject || 'Portfolio Inquiry') %>" class="btn-reply">
                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><line x1="22" y1="2" x2="11" y2="13"></line><polygon points="22 2 15 22 11 13 2 9 22 2"></polygon></svg>
                    Reply
                  </a>
                </div>

                <div class="secondary-actions">
                  <!-- Archive Button -->
                  <button type="button" class="btn-archive" title="Move to Archives" onclick="handleAction('archive', '<%= req._id %>', 'Are you sure you want to archive this inquiry?')">
                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="21 8 21 21 3 21 3 8"></polyline><rect x="1" y="3" width="22" height="5"></rect><line x1="10" y1="12" x2="14" y2="12"></line></svg>
                    Archive
                  </button>

                  <!-- Delete Button -->
                  <button type="button" class="btn-delete" title="Delete Inquiry" onclick="handleAction('delete', '<%= req._id %>', 'Are you sure you want to permanently delete this inquiry? This cannot be undone.')">
                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="3 6 5 6 21 6"></polyline><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"></path><line x1="10" y1="11" x2="10" y2="17"></line><line x1="14" y1="11" x2="14" y2="17"></line></svg>
                    Delete
                  </button>
                </div>
              </div>
            </div>
          <% }) %>
        </div>

        <!-- Pagination Controls -->
        <% if (totalPages > 1) { %>
          <div class="pagination">
            <% if (page > 1) { %>
              <a href="/admin/dashboard?page=<%= page - 1 %>&search=<%= encodeURIComponent(search) %>" class="page-link">&laquo; Prev</a>
            <% } %>

            <% for (let i = 1; i <= totalPages; i++) { %>
              <a href="/admin/dashboard?page=<%= i %>&search=<%= encodeURIComponent(search) %>" class="page-link <%= page === i ? 'active' : '' %>"><%= i %></a>
            <% } %>

            <% if (page < totalPages) { %>
              <a href="/admin/dashboard?page=<%= page + 1 %>&search=<%= encodeURIComponent(search) %>" class="page-link">Next &raquo;</a>
            <% } %>
          </div>
        <% } %>
      <% } %>
    </section>
  </main>

  <script>
    async function handleAction(action, id, confirmMessage) {
      if (confirmMessage && !confirm(confirmMessage)) {
        return;
      }

      try {
        const response = await fetch(`/admin/contact/${id}/${action}`, {
          method: 'POST',
          credentials: 'same-origin',
          headers: {
            'Accept': 'application/json'
          }
        });

        const result = await response.json();
        if (result.success) {
          const card = document.getElementById(`card-${id}`);
          if (card) {
            // Smooth fadeout animation
            card.style.transition = 'opacity 0.4s ease, transform 0.4s ease';
            card.style.opacity = '0';
            card.style.transform = 'translateY(15px) scale(0.95)';
            
            setTimeout(() => {
              card.remove();
              
              // Update badge counts dynamically
              const countBadges = document.querySelectorAll('.badge-count');
              const statsValue = document.querySelector('.stats-value');
              
              let newCount = 0;
              countBadges.forEach(badge => {
                newCount = Math.max(0, parseInt(badge.textContent) - 1);
                badge.textContent = newCount;
              });

              if (statsValue) {
                statsValue.textContent = newCount;
              }
              
              // If page is empty after deletion, refresh to reload pagination structure
              const list = document.querySelector('.requests-list');
              if (list && list.children.length === 0) {
                location.reload();
              }
            }, 400);
          }
        } else {
          alert(result.message || 'Operation failed');
        }
      } catch (err) {
        console.error('Action error:', err);
        alert('An error occurred. Please try again.');
      }
    }
  </script>

</body>
</html>
